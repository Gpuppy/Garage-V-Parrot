{% extends 'base.html.twig' %}

{% block title %}Vincent Parrot Contacte!{% endblock %}

{% block body %}

<div class="container">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <h1>Formulaire de Contact</h1>
 {{ form(form) }}
</div>
{% endblock %}
